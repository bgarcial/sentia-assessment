# Starter pipeline
- task: HelmDeploy@0
  inputs:
    azureSubscription: 'AzurePersonalSubscription'
    azureResourceGroup: 'sentia-assessment-testing'
    kubernetesCluster: 'WordpressSentiaAssessment-aks'
    command: 'package'
    chartPath: Deployments/HelmCharts/wordpress
    chartVersion: '$(build.buildId)'
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml


